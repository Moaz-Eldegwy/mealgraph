# Core agent framework
langgraph>=0.2.50,<0.3
langchain-core>=0.3.20,<0.4

# LLM provider (Gemini)
google-genai>=0.3.0

# Schemas / settings
pydantic>=2.9,<3
pydantic-settings>=2.6,<3

# Optimization (meal-quantities solver)
pulp>=2.9,<3

# Web search fallback
# No upper bound: PyPI for Python 3.13 only publishes ddgs 9.x; the
# DDGS().text() API used in tools.py is unchanged across 6.x→9.x.
ddgs>=6.3

# JSON repair fallback (kept until Phase 1 makes it a measured fallback)
json-repair>=0.30

# Env loading
python-dotenv>=1.0,<2

# Markdown rendering for notebook display (kept for backwards compat)
ipython>=8.0

# Demo UI (Hugging Face Space entry point — Phase 7).
# Pinned to a wide range; the app.py shape supports messages-format Chatbot
# in v4.40+ all the way through v6.x.
gradio>=4.40,<7

# Tests / dev
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
ruff>=0.7
